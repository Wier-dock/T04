#include <stdio.h>

int main() {
    int limit;

    printf("Введите предел: ");
    scanf("%d", &limit);

    for (int i = 1; i <= limit; i += 2) {
        printf("%d ", i);
    }

    printf("\n");

    return 0;
}
